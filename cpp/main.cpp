#include <iostream>
#include <fstream>
#include <string>

int main() {
    // Leer el archivo de entrada (aunque no lo usemos)
    std::ifstream input_file("./data/input/data.txt");

    // Simplemente imprimir Hello World
    std::cout << "Hello World" << std::endl;

    return 0;
}
